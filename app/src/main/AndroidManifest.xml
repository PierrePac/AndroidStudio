<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.greenvillage">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenVillage"
        tools:targetApi="31">


        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.CategoryActivity"
            android:label="@string/category_activity_title"
            android:parentActivityName=".activities.MainActivity"/>

        <activity
            android:name=".activities.SubCategoryActivity"
            android:label="@string/sub_category_activity_title"
            android:parentActivityName=".activities.CategoryActivity"/>

        <activity
            android:name=".activities.ProductsListActivity"
            android:label="@string/product_list_activity_title"
            android:parentActivityName=".activities.SubCategoryActivity"/>

        <activity
            android:name=".activities.ProductDetailActivity"
            android:label="@string/product_detail_activity_title"
            android:parentActivityName=".activities.ProductsListActivity"/>

    </application>

</manifest>